<%= form_for(chatroom_user) do |f| %>
  <% if chatroom_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chatroom_user.errors.count, "error") %> prohibited this chatroom_user from being saved:</h2>

      <ul>
      <% chatroom_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :chatroom_id %>
    <%= f.text_field :chatroom_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
